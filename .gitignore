# Build output and coverage artifacts. Everything else here is source.
/cschargen
/coverage.out
/coverage.counts.out

# Setting data transcribed from the rulebook. World, subsector and species
# names are Product Identity and are never committed here -- see CLAUDE.md,
# "The Product Identity boundary". setting/sample.json is invented and is
# the only setting file this repository may hold.
/setting.json
/setting.*.json
/data/
