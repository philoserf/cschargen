# prettier explodes the commented flow sequences this file uses to justify
# each linter disable, turning a readable argument into one entry per line.
.golangci.yml

# The coverage ratchet is generated by `task ratchet:update` and compared
# byte for byte by `task ratchet`. Reformatting it would fail the gate.
coverage.ratchet
coverage.out
coverage.counts.out

# Golden renders, compared byte for byte by `go test ./render/`. Prettier
# would reformat the tables and every comparison would fail.
render/testdata/

# Ignored through the global core.excludesfile, which prettier does not read.
.issues/
