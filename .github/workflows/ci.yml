name: CI

# CI runs exactly `task` -- the same gate a developer runs locally, with the
# same deliberately unpinned toolchain. When a tool moves and the gate goes
# red on untouched code, that is the signal working: answer the finding, do
# not pin the tool to silence it.
#
# No paths-ignore. A gate that skips a documentation-only push cannot catch
# a broken example, and the page cites in this repository are checked by
# tests that a documentation change can invalidate.

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  gate:
    # Pinned, where the toolchain above is deliberately not. The label
    # ubuntu-latest migrates to Ubuntu 26 from 19 October 2026 and warns
    # about it on every run; an image is not a tool this gate is testing,
    # so it is named rather than drifted into. Bump it deliberately.
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-go@v7
        with:
          # Deliberately unpinned, as above.
          go-version: stable

      # The official action installs the released binary. `go install` is
      # not a supported install path for golangci-lint and can produce a
      # build that behaves differently from the release -- which, on a gate
      # running `default: all`, would mean CI and a developer's machine
      # disagreeing about what the rules are.
      #
      # install-only, because CI runs exactly `task`, and `task lint` is
      # what invokes golangci-lint. v8 dropped that input and ignored it
      # with a warning, so the action was linting the tree itself and `task
      # lint` was doing it again; v9 restored it.
      - uses: golangci/golangci-lint-action@v9
        with:
          version: latest
          install-only: true

      - name: Install nilaway
        run: go install go.uber.org/nilaway/cmd/nilaway@latest

      - name: Install prettier
        run: npm install --global prettier

      - uses: arduino/setup-task@v3
        with:
          version: 3.x
          repo-token: ${{ secrets.GITHUB_TOKEN }}

      # TEMPORARY, for one run: what the runner is, and where the Gate's
      # time actually goes. Two invocations rather than seven, because
      # `ratchet` declares deps: [test] and `test` is run: once per
      # invocation -- timing each task alone would run the suite twice.
      - name: Measure
        run: |
          echo "::notice::cores=$(nproc) mem=$(free -g | awk '/Mem:/{print $2}')Gi"
          s=$(date +%s); task tidy vet lint nilaway docs; e=$(date +%s)
          echo "::notice::everything-but-the-suite $((e-s))s"
          s=$(date +%s); task test ratchet; e=$(date +%s)
          echo "::notice::the-suite $((e-s))s"
